-parseFloat.
- destruct of array.

AAA
-try it without parseFloat to see the difference.
Also this will show 'blue' or 'black' colored font on the console of browser accordingly.
- see how we are passing "map" over a single item. We can use this to manipulate the item by passing any function instead of parseFloat.

BBB
-reduce method.
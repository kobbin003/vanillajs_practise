*** console.table();

*** array.filter();

*** array.map(); --->>> returns a new array of the same length.

*** array.sort(compareFunction);
- return 1 and return -1 ????

https://en.wikipedia.org/wiki/Category:Boulevards_in_Paris

Selection from wikipedia
-queryselector() from any DOM object not just from document.

-[...nodeList]
OR
 Array.from(nodeList)
To transform nodeList to an Array.

-two consecutive methods e.g map().filter();